# Local runs only. Copy to `.env` (never commit it). On GitHub use
# Settings -> Secrets and variables -> Actions instead.

# Required
METACULUS_TOKEN=
OPENROUTER_API_KEY=

# AskNews (optional; either OAuth client id + secret, or an API key)
ASKNEWS_CLIENT_ID=
ASKNEWS_SECRET=
ASKNEWS_API_KEY=

# Optional behaviour switches (defaults in metabot/config.py)
# BOT_PROFILE=standard          # force a profile for the seasonal tournament
# MINIBENCH_PROFILE=economy
# TEST_PROFILE=economy
# MAX_COST_PER_RUN_USD=40
# LOG_FORECASTS=true            # print forecasts (local dry runs only!)
